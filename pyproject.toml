[project]
name = "churn-prediction-api"
version = "1.0.0"
description = "FastAPI application for churn prediction using MLflow"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    "mlflow>=2.10.0",
    "scikit-learn>=1.3.0",
    "pydantic>=2.0.0",
]
